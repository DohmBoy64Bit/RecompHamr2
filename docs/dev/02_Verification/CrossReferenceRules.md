# Cross-Reference Rules

Every command, tool, config key, MCP setting, skill, generated file, exported symbol, and user-visible behavior must cross-reference source, docs, tests, and parity or traceability rows.

# Architecture Overview

RecompHamr 2.0 is a Go terminal application with strict boundaries between app wiring, TUI, commands, agent loop, LLM client, context packing, tools, MCP, skills, config, project workspace, security, docs checks, and test harnesses.

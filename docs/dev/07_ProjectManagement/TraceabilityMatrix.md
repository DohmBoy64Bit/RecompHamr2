# Traceability Matrix

| Requirement | Source | Implementation | Verification |
|---|---|---|---|
| Mandatory memory refresh | `AGENTS.md` | Process rule | `make docscheck` verifies memory docs exist. |
| Phase goal packets | User plan and workflow goal system | `docs/dev/04_Workflows/PhaseGoals.md` | `make docscheck`; manual memory refresh. |
| Next 20 phase roadmap reconciliation | User-provided Next 20 Phases plan; workflow phase split | `docs/dev/07_ProjectManagement/Next20PhasePlan.md`; `PhaseGoals.md`; status reports | `make docscheck`; `make verify`. |
| Diagnostic-only runtime | Workflow Phase 2 | `cmd/recomphamr`, `internal/app` | `go test ./...`, diagnostic command. |
| Phase 2 support infrastructure | Workflow Phase 2 | `internal/logging`; `internal/testharness`; architecture docs | `make verify`. |
| Phase 0 prior waiting state | Workflow Phase 0 | Historical memory docs | Superseded by source inventory row. |
| No product features before parity | Workflow freeze | Corrective Phase 28 runtime integration only; post-parity feature intake remains blocked | App tests verify live default launch and summary-only evidence mode. |
| Phase 0 inventory complete | User reference URL | `internal/parity`; inventory docs | `go test ./internal/parity`; `make verify`. |
| 100% coverage gate | Coverage policy | `internal/covercheck`; `Makefile` | `make verify`. |
| Separation of concerns gate | User direction; architecture docs | `internal/archcheck`; `SeparationOfConcerns.md`; `Makefile` | `make archcheck`; `make verify`. |
| Config parity | Phase 3; RecompHamr 1.x `internal/config` evidence | `internal/config`; `configuration.md`; `ConfigParity.md` | `go test ./internal/config`; `make verify`. |
| Workspace parity subset | Phase 3; RecompHamr 1.x `internal/project` evidence | `internal/project`; `memory.md`; `MemoryParity.md` | `go test ./internal/project`; `make verify`. |
| Memory prompt injection subset | Phase 11 reference behavior | `project.LoadMemory`; `llm.WithProjectMemory`; `agent.Loop` memory fields; `memory.md`; `MemoryParity.md` | project, LLM, and agent memory tests; `make verify`. |
| LLM streaming parity | Phase 4; RecompHamr 1.x `internal/llm` evidence | `internal/llm`; `Protocols.md`; `ErrorHandling.md` | `go test ./internal/llm`; `make verify`. |
| Context packing parity | Phase 4; RecompHamr 1.x `internal/ctx` evidence | `internal/llm` packing helpers; `ContextPacking.md` | context packing tests; `make verify`. |
| Provider header/error parity | Phase 4; RecompHamr 1.x `internal/cloud` evidence | `BudgetFromHeaders`; `ContextWindowFromHeaders`; provider error classes | LLM header/error tests; `make verify`. |
| Tool schemas and Windows shell naming | Phase 5; user Windows-shell direction; RecompHamr 1.x `bash` evidence | `internal/tools`; `docs/user/tools.md`; `ToolRuntime.md`; `ToolParity.md` | `go test ./internal/tools`; `make verify`. |
| Tool security boundaries | Phase 5 mandatory gates | Shell timeout cap, cancellation handling, read truncation, cache path containment, GitHub URL validation, HTTP(S)-only reference fetches | Tool security tests; `make verify`. |
| Agent loop foundation | Phase 6; RecompHamr 1.x TUI loop/nudge evidence | `internal/agent`; `AgentLoop.md`; `docs/user/troubleshooting.md` | `go test ./internal/agent`; `make verify`. |
| Agent loop cancellation and no-infinite-loop policy | Phase 6 mandatory gates | Context checks, model-round cap, repeated-failure nudge, runaway-tool nudge, empty-reply blocked retry, verification nudge | Agent loop tests; `make verify`. |
| TUI shell foundation | Phase 7; RecompHamr 1.x TUI behavior inventory | `internal/tui`; `TUIArchitecture.md`; `TUIParity.md` | `go test ./internal/tui`; `make verify`. |
| Unique opencode-inspired TUI | User direction; official OpenCode docs confirm terminal TUI workflow | `internal/tui` initiative layout, evidence rail, compact status band, Bubble Tea adapter | TUI golden/model tests; `make verify`. |
| TUI separation and debug redaction | Phase 7 mandatory gates | Pure TUI state, Bubble Tea adapter, `internal/security` redaction, no agent/tool ownership | `make archcheck`; TUI redaction tests; `make verify`. |
| Slash command foundation | Phase 8; RecompHamr 1.x slash registry evidence | `internal/commands`; `CommandParity.md`; `docs/user/commands.md` | `go test ./internal/commands`; `make verify`. |
| Command docs/help coverage | Phase 8 mandatory gates | Registry metadata, generated help, generated markdown, examples, side effects, error classes | Command help/docs tests; `make verify`. |
| Skills foundation | Phase 9; RecompHamr 1.x skill evidence | `internal/skills`; embedded skill markdown; `docs/user/skills.md`; `SkillParity.md` | `go test ./internal/skills`; `make verify`. |
| Skill-new workflow | Phase 9; RecompHamr 1.x `/skill-new` evidence | `skills.NewDraft`; `skills.ScaffoldCustomSkill`; `/skill-new` command fetch/cache flow | command and skills tests; `make verify`. |
| MCP JSON-RPC protocol foundation | Phase 10; RecompHamr 1.x `internal/mcp/protocol.go` evidence | `internal/mcp/protocol.go`; `MCPArchitecture.md`; `Protocols.md`; `MCPParity.md` | `go test ./internal/mcp`; `make verify`. |
| MCP stdio and HTTP transport foundation | Phase 10; RecompHamr 1.x stdio and streamable HTTP client evidence | `internal/mcp/transport.go`; fake stream tests; fake HTTP server tests | transport tests; `make verify`. |
| MCP manager lifecycle | Plan Phase 11; RecompHamr 1.x `internal/mcp/manager.go` evidence | `internal/mcp/manager.go`; `MCPArchitecture.md`; `MCPParity.md` | manager tests; `make verify`. |
| Live MCP agent integration | Corrective Phase 29; MCP parity and security gates | `internal/app` MCP schema merge, HTTP and stdio explicit autostart, persistent `.rehamr/mcp.json` merge, MCP tool dispatch through `internal/mcp.Manager`, active-skill gating, docs and known-limits updates | app MCP integration tests; mcp connector/config tests; `make verify`; summary command. |
| `/mcp` lifecycle command dispatch | Plan Phase 11; slash command parity evidence | `internal/commands`; manager-backed command environment | command tests; `make verify`. |
| Doctor local diagnostics | Phase 12; slash command parity evidence | `internal/doctor`; `/doctor` dispatch; `DoctorParity.md`; `docs/user/doctor.md` | doctor and command tests; `make verify`. |
| Release asset naming, local binary builds, archive creation, checksum manifest generation, and checksum verification | Phase 12; release criteria and workflow evidence | `internal/release`; `ReleaseParity.md`; `ReleaseWorkflow.md`; `docs/user/install.md` | release package tests; `make verify`. |
| Install scripts, release config, devcontainer, CI, and self-update dry-run | Phase 12 operational parity | `scripts/install.ps1`; `scripts/install.sh`; `.goreleaser.yaml`; `.devcontainer/devcontainer.json`; `.github/workflows/verify.yml`; `internal/release`; `internal/update` | release, update, doctor, and archcheck tests; `make verify`. |
| Product runtime wiring | Next 20 Phase 14 and corrective Phases 28-29 app wiring contract | `cmd/recomphamr`; `internal/app`; `internal/config`; `internal/project`; `internal/commands`; `internal/mcp`; `internal/tui`; `internal/tools`; `internal/llm`; `internal/agent` | app and command-line tests; bare startup launch test; MCP live-agent tests; summary command; diagnostic command; `make verify`. |
| Interactive fake runtime smoke | Next 20 Phase 15; runtime hardening plan | `internal/app.RunSmoke`; injected fake model and tool runner; composed TUI slash command dispatch | app smoke tests for slash command, prompt/tool loop, cancellation, memory injection, transcript render; `make verify`. |
| Full parity matrix closure | Next 20 Phase 19; Workflow Phase 13 | `docs/dev/05_FeatureParity/ParityClosure.md`; updated parity docs and docs index | stale wording scan; placeholder-policy scan; `make verify`; diagnostic command. |
| Security hardening audit | Next 20 Phase 20; Workflow Phase 13 security audit | `internal/project` symlink refusal; `SecurityAudit.md`; updated security docs | project symlink regression tests; security keyword scan; `make verify`; diagnostic command. |
| Documentation coverage hardening | Next 20 Phase 21; Workflow Phase 13 docs coverage | `internal/docscheck`; docs coverage policy; updated user docs terms | docscheck 100% coverage; `make docscheck`; `make verify`; diagnostic command. |
| Cross-platform validation | Next 20 Phase 22; Workflow Phase 13 platform validation | `PlatformMatrix.md`; release target matrix; PowerShell/pwsh shell selection docs; install script marker validation; pure TUI render contract | targeted platform package tests; `go env GOOS GOARCH`; `make verify`; diagnostic command; placeholder-policy scan. |
| Performance and local-first budgeting | Next 20 Phase 23; Workflow Phase 13 performance baseline | Benchmarks in `internal/llm`, `internal/tui`, `internal/mcp`, `internal/tools`, and `internal/app`; `PerformanceBenchmarks.md` | benchmark command with `-benchmem`; affected package coverage tests; `make verify`; diagnostic command. |
| User walkthrough and migration guide | Next 20 Phase 24; Workflow Phase 14 release candidate prep | `docs/user/walkthrough.md`; `docs/user/migration.md`; README and docs index links | `make verify`; diagnostic command; placeholder-policy scan; docs hash comparison. |
| Release candidate preparation | Next 20 Phase 25; Workflow Phase 14 RC prep | `ReleaseCandidate.md`; `KnownLimits.md`; user RC notes; changelog; release workflow and criteria docs | release/update/doctor package coverage tests; `make verify`; diagnostic command; placeholder-policy scan; docs hash comparison. |
| RC soak and bugfix freeze | Next 20 Phase 26; Workflow Phase 14 stabilization | `RCSoak.md`; updated roadmap, parity, status, and known-limit memory | repeated `make verify`; diagnostic command; release/update/doctor coverage tests; placeholder-policy scan. |
| Stable release gate | Next 20 Phase 27; stable publication gate | `StableRelease.md`; updated roadmap, parity, status, release criteria, and known limits | `make verify`; local artifact build for six targets; `dist/SHA256SUMS` generation and verification; Windows installer smoke from `recomphamr_windows_amd64.zip`; published `v2.0.0`; successful CI; uploaded artifact and checksum URLs. |
| Live end-user runtime integration | Next 20 Phase 28 corrective runtime phase | `internal/app.Launch`; app-owned live Bubble Tea wrapper; stream model adapter; built-in tool dispatcher; `--summary`; docs and architecture boundary updates | Live TUI launch tests, command-flow tests, agent/tool/LLM runtime tests, cancellation tests, docs/help coverage, and `make verify`. |
| Live MCP agent integration | Next 20 Phase 29 corrective MCP runtime phase | `internal/app` live MCP schema exposure; MCP `server.tool` dispatch; explicit HTTP/stdio autostart; persistent MCP config; docs and architecture boundary updates | MCP schema exposure tests, manager dispatch tests, autostart tests, config tests, docs/help coverage, and `make verify`. |
| MCP stdio and persistent config correction | User corrective request after Phase 29 | `internal/mcp.StdioConnector`; `internal/mcp.AutoConnector`; `internal/mcp.LoadConfigFile`; `internal/mcp.MergeConfigs`; `internal/app.runtimeMCPConfigs`; MCP docs | stdio helper-process tests; config strict-decode tests; app persistent-config tests; `make verify`. |
| Stable publication evidence validation | User corrective request after Phase 29 and publication repair request | `internal/release.ValidatePublicationEvidence`; stable release docs and known limits; GitHub repository `DohmBoy64Bit/RecompHamr2`; release `v2.0.0` | publication evidence tests; `make verify`; successful CI `https://github.com/DohmBoy64Bit/RecompHamr2/actions/runs/29096150903`; release `https://github.com/DohmBoy64Bit/RecompHamr2/releases/tag/v2.0.0`; checksum URL. |
| Current roadmap pin | User request to pin current phase path before TUI hardening | `Next20PhasePlan.md`; `PhaseGoals.md`; `StatusReports.md` | docscheck; `make verify`; roadmap phase-number scan. |
| Corrective TUI rebuild phases 45-53 | User-supplied OpenCode reference, live RecompHamr screenshot, generated mock, Bubble Tea v2.0.8 docs, and local `tui-design` guidance | `Phase45TUIBaseline.md`; `PhaseGoals.md`; `Next20PhasePlan.md`; TUI architecture/spec/parity docs | Per-phase source tests, deterministic and live render evidence, docs hashes, and `make verify`. |
| Phase 46 TUI UX specification | Phase 45 baseline, supplied screenshots, Bubble Tea/Bubbles docs, and local TUI design guidance | `TUIUXSpec.md`; historical `TUISpec.md`; `TUIParity.md` | Cell-contract review; docscheck; `make verify`; pre/post docs hashes. |
| Phase 47 TUI architecture replacement | Bubble Tea v2.0.8 docs, Bubbles v2.1.1 textarea/viewport/help/key docs, and Phase 46 UX contract | `internal/tui` component and intent layer; `internal/app` intent consumption; `TUIArchitecture.md` | TUI/app 100% coverage; archcheck; `make verify`; docs hashes. |
| Phase 48 Bubble Tea runtime and input | Bubble Tea v2.0.8 message/View docs, Bubbles textarea/viewport docs, and Phase 46 input contract | `internal/tui` runtime adapter, component routing, terminal floor; TUI architecture and user key docs | Pure key/paste/focus/scroll/resize tests; TUI/app 100% coverage; `make verify`; docs hashes. |
| Phase 49 minimal startup experience | Phase 46 five-group startup and responsive contracts | shared plain/styled startup helpers; `internal/tui/testdata/startup_*.golden`; TUI architecture, UX, parity, and quickstart docs | 120x32, 80x24, and 60x20 goldens; app launcher integration; TUI/app 100% coverage; `make verify`. |
| Phase 50 command palette and pickers | Command registry, app-owned picker snapshots, Phase 46 overlay contract | bounded palette/detail renderer; typed picker intents; app intent consumption | Navigation, blocked/empty/compact, intent integration, TUI/app 100% coverage; `make verify`. |
| Phase 51 transcript and runtime feedback | Phase 46 runtime matrix; Bubble Tea viewport docs; app agent-result wiring | transcript follow state; bounded semantic blocks; `runtime_states.golden`; architecture/user docs | Scroll/new-output/redaction/cancellation tests; TUI/app 100% coverage; `make verify`. |
| Phase 52 responsive theme and accessibility | Bubble Tea color-profile messages; Lip Gloss profile completion; Phase 46 responsive/theme contract | immutable TUI theme; layout color profile; `color_profiles.golden`; accessibility/user docs | Five sizes x four profiles; Unicode, line-width, cursor, too-small tests; 100% coverage; `make verify`. |
| Corrective TUI hardening plan | User screenshots; workflow TUI design direction; OpenCode public docs as non-copying reference | Phases 30-33 in `Next20PhasePlan.md`; matching `PhaseGoals.md`; `TUIArchitecture.md` | future golden render tests, key handling tests, app smoke tests, docs coverage, and `make verify`. |
| Phase 30 TUI specification | Corrective TUI hardening Phase 30 | `TUISpec.md`; `TUIParity.md`; `Next20PhasePlan.md`; `StatusReports.md` | docscheck; `make verify`; OpenCode reference limited to broad structure observations. |
| Windows `.exe` end-user path | RecompHamr 1.x release expectation; Phase 12/27 release artifact evidence | Phase 34 in `Next20PhasePlan.md`; `docs/user/install.md`; `docs/user/quickstart.md`; release package helpers; installer docs | Phase 34 local `go build -trimpath` evidence for `%TEMP%\recomphamr-phase34\recomphamr.exe`; `--summary`; `--diagnostic`; local archive and `SHA256SUMS` verification; targeted release/update/app coverage; `make verify`. |
| Post-parity feature intake | Workflow Phase 15; Phase 35 goal packet | `FeatureIntake.md`; `Next20PhasePlan.md`; `StatusReports.md` | Intake register review; docscheck; `make verify`; no implementation before approved goal packets. |
| Final polished end-user TUI | User request for feature-complete polished TUI; existing Phase 30-33 TUI hardening docs | `internal/tui` pure renderer; `TUIArchitecture.md`; `TUISpec.md`; `docs/user/quickstart.md`; `docs/user/commands.md`; parity and status rows | `go test ./internal/tui ./internal/app -cover`; final `make verify`; docs hash comparison. |
| Bubble Tea v2 documentation and implementation rule | User request to read Bubble Tea docs and use Bubble Tea fully for TUI work; Bubble Tea v2 docs and upgrade guide | `AGENTS.md` Bubble Tea TUI rule; `internal/tui.BubbleModel`; `internal/app.liveModel`; `go.mod`; TUI architecture/spec/user docs | Bubble Tea v2 view-field tests, key/paste event tests, docscheck, `go test ./internal/tui ./internal/app -cover`, and `make verify`. |
| Ground-up OpenCode-inspired TUI redesign | User screenshot feedback and request for complete redesign; non-copying OpenCode-inspired direction; Bubble Tea v2 docs | `internal/tui` centered launcher, transcript-first chat, floating palette, bottom composer/status, compact layout, Lip Gloss color roles; TUI docs and user docs | TUI layout/state tests, Bubble Tea styled view tests, `go test ./internal/tui ./internal/app -cover`, docscheck, and `make verify`. |
| TUI startup launcher regression | Screenshot-verified launch defect; TUI spec startup/welcome requirement | `internal/app.ComposeRuntime` keeps transcript empty on bare composition while preserving status layout evidence | `TestComposeRuntimeStartsOnLauncher`; `go test ./internal/tui ./internal/app -cover`; `make verify`. |
| TUI startup layout correction | User screenshot showing stray cursor and unbalanced launcher placement; Bubble Tea v2 declarative cursor rule | `internal/tui` anchored startup layout, launcher width/top-padding helpers, styled composer cursor helpers | `TestBubbleTeaStartupLayoutIsAnchored`; `go test ./internal/tui ./internal/app -cover`; `make verify`. |
| Corrective TUI rewrite governance pin | User request to preserve the old roadmap and fully rewrite the rejected TUI through Bubble Tea v2 | Phases 37-44 in `Next20PhasePlan.md`; matching `PhaseGoals.md`; TUI parity/spec/architecture docs; status report and docs index updates | `make docscheck`; `make verify`; before/after docs hash comparison. |
| Bubble Tea v2 mandatory rewrite rule | User requirement to read `https://pkg.go.dev/charm.land/bubbletea/v2` before every TUI implementation task | `AGENTS.md` Bubble Tea rule; `TUIArchitecture.md`; Phase 37-44 goal packets | Current pkg.go.dev review; future TUI phase evidence; `go test ./internal/tui ./internal/app -cover`; `make verify`. |
| Phase 38 TUI design audit and non-copy spec | User screenshot rejection; Bubble Tea v2 docs; temporary OpenCode reference checkout; local `internal/tui` source audit | `TUISpec.md` Phase 38 audit, color roles, two-lane layout, screen contracts, and implementation constraints; `TUIArchitecture.md`; `TUIParity.md` | `make docscheck`; `make verify`; OpenCode checkout commit `d0ba5389248e05546849b9f69b7bc417aa5fd5d7`; before/after docs hash comparison. |
| Phase 39 TUI architecture reset | Phase 38 design spec; separation-of-concerns rule; Bubble Tea v2 docs | `internal/tui` split into focused files for model/update, Bubble Tea adapter, composer, layout, plain render, styled render, palette, status, transcript, and styles; TUI architecture/parity/status docs | `go test ./internal/tui ./internal/app -cover`; `make archcheck`; `make verify`. |
| Phase 40 Bubble Tea shell rebuild | Corrective TUI rewrite plan; Bubble Tea v2 docs; Phase 39 split architecture | `internal/tui.BubbleModel.View`; `styledScreen`; Bubble Tea key/paste/resize adapter; declarative `tea.View` terminal fields; TUI architecture/parity/status docs | `go test ./internal/tui ./internal/app -cover`; `make verify`; Bubble Tea docs review. |
| Phase 41 startup and composer experience | Corrective TUI rewrite plan; `TUISpec.md` startup screen contract; Bubble Tea v2 docs | `internal/tui` styled startup brand selection, safety line, memory-aware status row, local-permission composer row, styled cursor placement, quickstart and parity/status docs | `go test ./internal/tui ./internal/app -cover`; `make verify`; Bubble Tea docs review. |
| Phase 42 command palette and modal overlays | Corrective TUI rewrite plan; `TUISpec.md` palette/model/skill/MCP/help contracts; Bubble Tea v2 docs | `internal/commands.PickerRow`; model/skill/MCP picker snapshots; `internal/tui` overlay rows, selection, modal titles, help rows, docs updates | `go test ./internal/commands ./internal/tui ./internal/app -cover`; `make archcheck`; `make verify`; Bubble Tea docs review. |
| Phase 43 transcript and runtime states | Corrective TUI rewrite plan; `TUISpec.md` transcript/runtime-state contract; Bubble Tea v2 docs | `internal/tui` transcript classification, verification/status labels, display-time redaction for transcript/status/debug output, plain and styled runtime renders, quickstart and parity/status docs | `go test ./internal/tui ./internal/app ./internal/agent -cover`; `make verify`; Bubble Tea docs review. |
| Phase 44 screenshot verification and release smoke | Corrective TUI rewrite plan; `TUISpec.md` screenshot acceptance criteria; Bubble Tea v2 docs | Deterministic TUI render captures under `docs/dev/07_ProjectManagement/phase44_screenshots/`; local `dist/recomphamr.exe`; `Phase44TUIScreenshotEvidence.md`; parity/status docs | Screenshot evidence review; `go build -trimpath -o dist/recomphamr.exe ./cmd/recomphamr`; `dist/recomphamr.exe --summary`; `dist/recomphamr.exe --diagnostic`; `make verify`. |
| Phase 36 extension architecture planning | `FeatureIntake.md` FI-001; workflow Phase 15 post-parity planning | `ExtensionArchitecture.md`; `ModuleContracts.md`; `SeparationOfConcerns.md`; docs index; status and roadmap rows | `make docscheck`; `make verify`; no runtime implementation added. |
| Phase 54 Bubble Tea replacement baseline | User screenshots; rejected Phase 53 interaction; local TUI/app source and tests | phases 54-62 goal packets; `Phase45TUIBaseline.md` replacement inventory; roadmap, parity, and status memory | source/import inventory; pre/post docs hashes; `make docscheck`; `make verify`. |
| Phase 55 replacement UX and contract | Phase 54 inventory; Bubble Tea/Bubbles docs; TUI design guidance; user-approved replacement plan | `TUIUXSpec.md` state ownership, geometry, interactions, rationale, and manual acceptance; `TUIArchitecture.md` typed message flow | docs hashes; `make docscheck`; `make verify`; no runtime implementation. |
| Phase 56 atomic TUI architecture reset | Phase 55 ownership contract; Bubble Tea/Bubbles v2 docs | replacement `internal/tui` component model; `tui.Snapshot`, `IntentMsg`, and `TranscriptMsg`; message-driven `internal/app` adapter; obsolete source and fixtures removed | focused TUI/app tests at 100%; `make archcheck`; `make verify`. |
| Phase 57 authoritative composer | User-reported placeholder and Backspace defect; Bubbles textarea docs | sole textarea value/cursor ownership; removable bare slash; multiline and history behavior; quickstart and architecture docs | Bubble Tea key/paste sequence tests; TUI 100% coverage; `make verify`. |
| Phase 58 authoritative transcript | Phase 58 goal packet; Bubbles viewport documentation; semantic transcript and redaction requirements | viewport-owned scrolling/follow; paused-reader feedback; normalized semantic blocks; bounded tool/MCP output; architecture, parity, quickstart, and status docs | append/follow/pause/scroll/truncation/redaction/profile tests; `make verify` at 100%. |
| Phase 59 authoritative palettes and pickers | Phase 59 goal packet; Bubbles list documentation; command registry and picker-row APIs | list-owned filtering, selection, navigation, pagination, and empty states; typed command/model/skill/MCP acceptance; argument and help completion; architecture, parity, quickstart, and status docs | filter-message, arrow, `j`/`k`, blocked, empty, completion, and intent tests; `make verify` at 100%. |
| Phase 60 responsive layout and theme | Phase 60 goal packet; Lip Gloss v2 and Bubble Tea docs; `TUIUXSpec.md`; TUI design visual and interaction guidance | measured single-lane layout; original wide/compact wordmarks; semantic custom list delegate; ANSI16/ANSI256/truecolor/NO_COLOR themes; Unicode-safe padding and cursor bounds | five-size frame assertions; four-profile tests; CJK/combining/long-value tests; `make verify` at 100%. |
| Phase 61 runtime integration | Phase 61 goal packet; Bubble Tea command/message docs; app/TUI separation contract | exact-once typed submit, command, model, skill, MCP, cancel, and quit adapter; immutable snapshot and semantic transcript return path; no widget access from app | dedicated intent-boundary test; fake agent/tool/MCP, blocked, cancellation, and exit integration tests; `make archcheck`; `make verify` at 100%. |
| Phase 62 manual TUI acceptance and release evidence | Phase 62 goal packet; replacement source/tests; Bubble Tea v2 docs; manual-only screenshot rule | profile-safe Bubbles chrome; `dist/recomphamr.exe`; `Phase62TUIAcceptanceEvidence.md`; roadmap, parity, architecture, stable-release, and status memory | component-profile regression tests; `make verify` at 100%; executable summary/diagnostic smoke; SHA-256 and metadata; six user-captured WezTerm screens pending. |
| Phase 62 focus-safe first render correction | User-provided production panic stack; Bubbles textarea cursor contract; Bubble Tea focus reporting | optional cursor handling in startup/chat; app-boundary regression; troubleshooting, architecture, parity, status, and executable evidence | blurred startup/chat tests; blur-before-first-render app test; `make verify` at 100%; rebuilt executable smoke and SHA-256; user launch confirmation pending. |
| Phase 53 manual visual acceptance correction | User-rejected startup, chat, and palette screenshots; Phase 46 UX contract; Bubble Tea v2 docs | bounded chat lane, fixed composer, anchored overlays, normalized transcript labels, final-frame cursor calculation, user and architecture docs | `TestPhase53LayoutCoherence`; `make verify` at 100%; fresh Windows executable SHA-256 `2eb5301863f78f833d703ff1019b82bac3cf8e41e9582f2d329cf76b86b18eeb`; summary and diagnostic smoke pass; user-captured startup, palette, and active-transcript screenshots pending. |

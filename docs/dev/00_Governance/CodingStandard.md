# Coding Standard

Go code must be formatted with `gofmt`. Package names are lowercase and specific. Exported identifiers require doc comments beginning with the identifier name. Unsupported behavior must return explicit unsupported errors, not silent success.

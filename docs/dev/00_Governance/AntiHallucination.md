# Anti-Hallucination Policy

All claims require evidence from local source, docs, tests, runtime output, or authoritative external sources. If evidence is missing, label the claim `unverified`, `unsupported`, or `blocked`.

Do not infer command behavior, config defaults, model behavior, MCP behavior, or reverse-engineering facts without evidence.

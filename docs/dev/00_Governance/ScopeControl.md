# Scope Control

New product features are blocked until parity is complete. Phase work must stay inside the phase goal and record out-of-scope requests in `docs/dev/01_Memory/ProblemRegistry.md` or the traceability matrix.

# Security Policy

Security is part of parity. Required controls include symlink refusal, traversal rejection, owner-only sensitive files, atomic writes, cancellation, timeout enforcement, redaction, MCP gating, no silent cloud fallback, and documented network access.

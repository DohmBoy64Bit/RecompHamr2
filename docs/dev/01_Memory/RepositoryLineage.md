# Repository Lineage

## RecompHamr2 Feature Reference

`DohmBoy64Bit/RecompHamr2` is the verified feature-reference implementation for
future ports. The implementation baseline is commit
`20727e5ebf4fb8492abd5dee77d70f3c020f1970`. The annotated tag
`recomphamr2-feature-reference-2026-07-13` points to Phase 63's lineage checkpoint
`888c20f`, which adds documentation without changing product code. Its TUI is
historical evidence and is not the presentation implementation to port.

## Legacy RecompHamr

The original `DohmBoy64Bit/RecompHamr` history is preserved by renaming it to
`DohmBoy64Bit/RecompHamr-Legacy` and archiving it. Its verified inventory commit
is `259a450e93af48437ee23663e5ca66cdc1ab8569`.

## New RecompHamr Foundation

The new `DohmBoy64Bit/RecompHamr` repository is derived through GitHub's fork
relationship from `codehamr/codehamr`. The imported upstream baseline is pinned
to commit `85409d167b97bec64ee330d51872d358d3ce2d57`.

CodeHamr is distributed under the MIT License. The upstream license and
copyright notice must remain in derived source distributions. Product-facing
CodeHamr and HamrPass references may be removed during later phases, but legal
attribution and this provenance record are permanent.

## Evidence Policy

Repository URLs, archive state, fork relationships, default branches, tags, and
commit identities are runtime facts. Verify them with Git and GitHub metadata;
do not infer them from this document when the remote state may have changed.
